class Scollbars extends Component {
  constructor(props) {
    super(props);
  }

  componentDidMount() {
  }

  componentWillMount() {
  }

  componentDidMount() {

  }

  componentWillReceiveProps(nextProps) {
    //nextProps.productCode !== this.props.productCode && store.dispatch((0, _actions.getInitialState)(nextProps.productCode))
  }

  render() {
    return (
      <div data-reactroot="" class="Modal Modal--blindbuilder open">
		    <div class="Modal__background"></div>
		    <div class="Modal__content">

        </div>
      </div>
    )
  }
}

export default Scollbars
